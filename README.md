# PushChain Bot

A simple automation script for interacting with the PushChain network. This bot is provided by **Cuitme(cuan with me)**.

## 🚀 Features

* Simulate Transactions
* Launch a new Token
* Register a `.push` Domain

## 🔧 Setup

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/cuitme/pushChain-bot.git
    cd pushChain-bot
    ```

2.  **Install dependencies:**
    ```bash
    npm install
    ```

3.  **Configure Environment:**
    * Create a `.env` file.
    * Add your wallet private keys (`PRIVATE_KEY_1`, `PRIVATE_KEY_2`, ...).

4.  **Run the bot:**
    ```bash
    node index.js
    ```
